# =============================================================================
# Environment Configuration for TextMining & LLMs Notebooks
# =============================================================================
# Copy this file to .env and customize with your values:
#   cp .env.example .env
# =============================================================================

# Remote Server Configuration
REMOTE_SERVER=137.204.107.40
REMOTE_USER=molfetta
SSH_PORT=22

# Jupyter Port Configuration
LOCAL_PORT=50000        # Port on your local machine where Jupyter will be accessible
REMOTE_PORT=50000       # Port inside the Docker container running Jupyter

# Remote Workspace (uses $HOME/workspace by default for proper permissions)
WORKSPACE_PATH=$HOME/workspace

# HuggingFace API Token (optional, for downloading gated models)
HF_TOKEN=

# GPU Configuration
# Specify which GPU(s) to use. Examples:
#   0       - Use GPU 0 only (default)
#   1       - Use GPU 1 only
#   0,1     - Use GPUs 0 and 1
#   all     - Use all available GPUs
CUDA_VISIBLE_DEVICES=0
